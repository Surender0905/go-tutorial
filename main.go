package main

import (
	"fmt"
)

func main() {
    // Print the message "learning go language"
    fmt.Println("learning go language")

    // Declare and initialize variables
    var name string = "roshan"
    var version = "version latest"
    var decided bool = false
    var length = 11.22


	person := "rakesh"
    // Print the values of the variables
    fmt.Println(name, version, decided, length, person)
}